to run this do the following:

write in terminal:

cd backend;
npm install;
npm run dev;

open another terminal:

cd frontend;
npm install;
npm run start;

ensure you do this in the parent directory containing backend and frontend